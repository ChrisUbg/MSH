# MSH Production Environment for Raspberry Pi
# These ports are chosen to avoid conflicts with existing containers

# Web application port (avoiding conflict with haustagebuch_web_1 on 8080)
WEB_PORT=8083

# Database port (avoiding conflict with existing postgres on 5432)
DB_PORT=5435

# Matter Bridge port (avoiding conflicts)
MATTER_BRIDGE_PORT=8085

# Database credentials
DB_USER=postgres
DB_PASSWORD=postgres

# Environment
ASPNETCORE_ENVIRONMENT=Production 